---
import content from '../content.json';

const { name, tagline, description, badges, features, steps, requirements, terminal } = content;
---

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content={`${name} - ${description}`} />
  <title>{name}</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,300;0,6..72,400;1,6..72,300&family=DM+Mono:wght@400&display=swap" rel="stylesheet">
</head>
<body>
  <div class="page">
    <div class="breath"></div>
    
    <header>
      <button class="mode-toggle" aria-label="Toggle light/dark mode">
        <span class="mode-icon sun">&#9728;</span>
        <span class="mode-icon moon">&#9790;</span>
      </button>
    </header>

    <main>
      <section class="hero">
        <div class="wordmark">{name}</div>
        <p class="tagline">{tagline}</p>
        <p class="desc">{description}</p>
        
        <div class="pills">
          {badges.map(b => <span class="pill">{b.label}</span>)}
        </div>

        <div class="ctas">
          <a href="https://github.com/alosec/yappatron" class="cta primary">github</a>
          <a href="#install" class="cta">get started</a>
        </div>
      </section>

      <div class="divider"><span></span></div>

      <section>
        <h2>features</h2>
        <div class="feature-list">
          {features.map(f => (
            <div class="feature-item">
              <h3>{f.title}</h3>
              <p>{f.body}</p>
            </div>
          ))}
        </div>
      </section>

      <div class="divider"><span></span></div>

      <section class="demo-section">
        <div class="video-player" id="video-player">
          <video id="demo-video" loop playsinline preload="metadata">
            <source src="/demo.mp4" type="video/mp4" />
          </video>
          <button class="play-btn" id="play-btn" aria-label="Play video">
            <svg viewBox="0 0 24 24" fill="currentColor"><polygon points="5,3 19,12 5,21"/></svg>
          </button>
          <div class="video-progress" id="video-progress">
            <div class="video-progress-bar" id="progress-bar"></div>
          </div>
        </div>
        <ol class="steps">
          {steps.map((s, i) => (
            <li>
              <strong>{s.label}</strong>
              <span>{s.detail}</span>
            </li>
          ))}
        </ol>
      </section>

      <div class="divider"><span></span></div>

      <section id="install">
        <h2>install</h2>
        <pre><code>git clone https://github.com/alosec/yappatron
cd yappatron
./scripts/run-dev.sh</code></pre>
        <h3>first run</h3>
        <p>On first launch, macOS will prompt for Microphone and Accessibility permissions. Grant both, then restart the script. If dictation isn't working, go to System Settings → Privacy & Security → Accessibility, remove yappatron with the minus button, then re-add it with plus.</p>
        <h3>requirements</h3>
        <ul>
          {requirements.map(r => <li>{r}</li>)}
        </ul>
      </section>

      <div class="divider"><span></span></div>

      <section>
        <h2>philosophy</h2>
        <blockquote>The best interface is no interface.</blockquote>
        <p>Voice dictation should be invisible. Think, speak, words appear. No buttons, no modes, no cognitive load.</p>
      </section>
    </main>

    <footer>
      <p>made for yappers · <a href="https://github.com/alosec/yappatron">github</a> · MIT · free forever</p>
    </footer>
  </div>

  <style>
    :root {
      --bg: #faf9f7;
      --surface: #ffffff;
      --text: #1a1a1a;
      --text-2: #666;
      --text-3: #999;
      --border: #e5e5e5;
      --accent: #a78bfa;
      --orb-opacity: 0.25;
      --radius: 4px;
    }

    /* Dark mode */
    [data-mode="dark"] {
      --bg: #0f0f0f;
      --surface: #1a1a1a;
      --text: #e5e5e5;
      --text-2: #999;
      --text-3: #666;
      --border: #2a2a2a;
      --orb-opacity: 0.35;
    }

    /* Light mode (explicit) */
    [data-mode="light"] {
      --bg: #faf9f7;
      --surface: #ffffff;
      --text: #1a1a1a;
      --text-2: #666;
      --text-3: #999;
      --border: #e5e5e5;
      --orb-opacity: 0.45;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      font-size: 18px;
      -webkit-font-smoothing: antialiased;
    }

    body {
      font-family: 'Newsreader', Georgia, serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.7;
      min-height: 100vh;
    }

    .page {
      max-width: 600px;
      margin: 0 auto;
      padding: 3rem 1.5rem 4rem;
      position: relative;
    }

    /* Breathing animation - the soul of yappatron */
    .breath {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 600px;
      height: 600px;
      background:
        radial-gradient(ellipse at 30% 30%, #ff0000 0%, transparent 50%),
        radial-gradient(ellipse at 70% 30%, #00ff00 0%, transparent 50%),
        radial-gradient(ellipse at 50% 70%, #0000ff 0%, transparent 50%);
      opacity: var(--orb-opacity);
      border-radius: 50%;
      pointer-events: none;
      animation: breathe 6s ease-in-out infinite, spin 20s linear infinite;
      z-index: -1;
      filter: blur(60px);
    }

    @keyframes breathe {
      0%, 100% { transform: translate(-50%, -50%) scale(1) rotate(0deg); opacity: var(--orb-opacity); }
      50% { transform: translate(-50%, -50%) scale(1.3) rotate(180deg); opacity: calc(var(--orb-opacity) * 1.4); }
    }

    @keyframes spin {
      from { transform: translate(-50%, -50%) rotate(0deg); }
      to { transform: translate(-50%, -50%) rotate(360deg); }
    }

    /* Header */
    header {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      gap: 1rem;
      margin-bottom: 4rem;
    }

    .mode-toggle {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 0.4rem 0.5rem;
      cursor: pointer;
      display: flex;
      align-items: center;
      transition: all 0.2s;
    }

    .mode-toggle:hover {
      border-color: var(--accent);
    }

    .mode-icon {
      font-size: 0.9rem;
      line-height: 1;
      transition: opacity 0.2s;
    }

    .mode-icon.sun {
      color: #f59e0b;
    }

    .mode-icon.moon {
      color: #94a3b8;
    }

    /* Light mode: show sun, hide moon */
    [data-mode="light"] .mode-icon.sun { opacity: 1; }
    [data-mode="light"] .mode-icon.moon { opacity: 0; position: absolute; }

    /* Dark mode: show moon, hide sun */
    [data-mode="dark"] .mode-icon.sun { opacity: 0; position: absolute; }
    [data-mode="dark"] .mode-icon.moon { opacity: 1; }

    /* Hero */
    .hero {
      text-align: center;
      margin-bottom: 1rem;
    }

    .wordmark {
      font-size: 3.5rem;
      font-weight: 300;
      letter-spacing: -0.03em;
      margin-bottom: 0.25rem;
      color: var(--text);
    }

    .tagline {
      font-size: 1.5rem;
      font-style: italic;
      font-weight: 400;
      color: var(--text);
      margin-bottom: 1rem;
    }

    .desc {
      color: var(--text);
      opacity: 0.85;
      max-width: 320px;
      margin: 0 auto 1.5rem;
      font-size: 1.05rem;
    }

    .pills {
      display: flex;
      gap: 0.5rem;
      justify-content: center;
      flex-wrap: wrap;
      margin-bottom: 2rem;
    }

    .pill {
      font-family: 'DM Mono', monospace;
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      padding: 0.4rem 0.75rem;
      border: 1px solid var(--text-3);
      border-radius: 3px;
      color: var(--text);
      font-weight: 500;
    }

    .ctas {
      display: flex;
      gap: 0.75rem;
      justify-content: center;
    }

    .cta {
      font-family: 'DM Mono', monospace;
      font-size: 0.8rem;
      padding: 0.65rem 1.25rem;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      color: var(--text);
      text-decoration: none;
      transition: all 0.15s;
    }

    .cta:hover {
      border-color: var(--accent);
      color: var(--accent);
    }

    .cta.primary {
      background: var(--text);
      color: var(--bg);
      border-color: var(--text);
    }

    .cta.primary:hover {
      background: var(--accent);
      border-color: var(--accent);
      color: var(--bg);
    }

    /* Sections */
    section {
      margin: 2.5rem 0;
    }

    h2 {
      font-size: 0.8rem;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: var(--text-2);
      margin-bottom: 1.5rem;
      font-family: 'DM Mono', monospace;
    }

    h3 {
      font-size: 1rem;
      font-weight: 400;
      margin: 1.5rem 0 0.5rem;
    }

    p {
      color: var(--text-2);
      margin-bottom: 1rem;
    }

    /* Divider */
    .divider {
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 3rem 0;
    }

    .divider span {
      width: 4px;
      height: 4px;
      background: var(--border);
      border-radius: 50%;
    }

    /* Features */
    .feature-list {
      display: grid;
      gap: 1.5rem;
    }

    .feature-item {
      padding-bottom: 1.5rem;
      border-bottom: 1px solid var(--text-3);
    }

    .feature-item:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }

    .feature-item h3 {
      font-size: 1.05rem;
      font-weight: 500;
      margin: 0 0 0.35rem;
      color: var(--text);
    }

    .feature-item p {
      margin: 0;
      font-size: 0.95rem;
      color: var(--text);
      opacity: 0.8;
    }

    /* Video Player */
    .video-player {
      position: relative;
      background: #000;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      margin-bottom: 1.5rem;
      overflow: hidden;
      aspect-ratio: 16 / 10;
      cursor: pointer;
    }

    .video-player video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .play-btn {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 72px;
      height: 72px;
      border-radius: 50%;
      border: none;
      background: rgba(255, 255, 255, 0.95);
      color: #000;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: transform 0.15s, opacity 0.15s;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.3);
    }

    .play-btn svg {
      width: 28px;
      height: 28px;
      margin-left: 4px;
    }

    .play-btn:hover {
      transform: translate(-50%, -50%) scale(1.08);
    }

    .video-player.playing .play-btn {
      opacity: 0;
      pointer-events: none;
    }

    .video-player:hover.playing .play-btn {
      opacity: 1;
      pointer-events: auto;
    }

    .video-player.playing .play-btn svg {
      display: none;
    }

    .video-player.playing .play-btn::before,
    .video-player.playing .play-btn::after {
      content: '';
      width: 8px;
      height: 24px;
      background: #000;
      border-radius: 2px;
      position: absolute;
    }

    .video-player.playing .play-btn::before {
      left: 50%;
      transform: translateX(-10px);
    }

    .video-player.playing .play-btn::after {
      left: 50%;
      transform: translateX(2px);
    }

    .video-progress {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: rgba(255, 255, 255, 0.2);
      opacity: 0;
      transition: opacity 0.2s;
    }

    .video-player:hover .video-progress,
    .video-player.playing .video-progress {
      opacity: 1;
    }

    .video-progress-bar {
      height: 100%;
      background: var(--accent);
      width: 0%;
      transition: width 0.1s linear;
    }

    /* Steps */
    .steps {
      list-style: none;
      counter-reset: step;
    }

    .steps li {
      counter-increment: step;
      display: flex;
      flex-direction: column;
      margin-bottom: 0.75rem;
      padding-left: 1.75rem;
      position: relative;
    }

    .steps li::before {
      content: counter(step);
      position: absolute;
      left: 0;
      font-family: 'DM Mono', monospace;
      font-size: 0.7rem;
      color: var(--text-3);
    }

    .steps strong {
      font-weight: 400;
      color: var(--text);
    }

    .steps span {
      font-size: 0.9rem;
      color: var(--text);
      opacity: 0.8;
    }

    /* Code */
    pre {
      font-family: 'DM Mono', monospace;
      font-size: 0.8rem;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1rem;
      overflow-x: auto;
      margin-bottom: 1.5rem;
    }

    code {
      font-family: inherit;
      color: var(--text);
      opacity: 0.9;
    }

    ul {
      list-style: none;
    }

    ul li {
      padding-left: 1rem;
      position: relative;
      color: var(--text);
      opacity: 0.85;
      margin-bottom: 0.5rem;
    }

    ul li::before {
      content: '·';
      position: absolute;
      left: 0;
      color: var(--text-2);
    }

    blockquote {
      font-style: italic;
      color: var(--text);
      opacity: 0.9;
      padding-left: 1rem;
      border-left: 2px solid var(--text-3);
      margin: 1.5rem 0;
    }

    section > p {
      color: var(--text);
      opacity: 0.85;
    }

    /* Footer */
    footer {
      margin-top: 4rem;
      text-align: center;
    }

    footer p {
      font-size: 0.85rem;
      color: var(--text-3);
    }

    footer a {
      color: var(--text-2);
      text-decoration: none;
    }

    footer a:hover {
      color: var(--accent);
    }

    /* Mobile */
    @media (max-width: 600px) {
      html { font-size: 16px; }
      
      .page { padding: 2rem 1.25rem 3rem; }
      
      header { margin-bottom: 3rem; }
      
      .wordmark { font-size: 2.75rem; }
      
      .tagline { font-size: 1.25rem; }
      
      .ctas { flex-direction: column; }
      
      .cta { text-align: center; }
      
      .breath { width: 350px; height: 350px; }
      
      .pill { font-size: 0.65rem; padding: 0.3rem 0.5rem; }
    }
  </style>

  <script>
    const modeToggle = document.querySelector('.mode-toggle');

    // Load saved mode preference (defaults to system preference)
    const savedMode = localStorage.getItem('yappatron-mode') ||
      (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');

    // Apply mode
    document.documentElement.dataset.mode = savedMode;

    // Light/dark mode toggle
    modeToggle.addEventListener('click', () => {
      const currentMode = document.documentElement.dataset.mode;
      const newMode = currentMode === 'dark' ? 'light' : 'dark';
      document.documentElement.dataset.mode = newMode;
      localStorage.setItem('yappatron-mode', newMode);
    });

    // Video player
    const player = document.getElementById('video-player');
    const video = document.getElementById('demo-video');
    const playBtn = document.getElementById('play-btn');
    const progressBar = document.getElementById('progress-bar');

    function togglePlay() {
      if (video.paused) {
        video.play();
        player.classList.add('playing');
      } else {
        video.pause();
        player.classList.remove('playing');
      }
    }

    player.addEventListener('click', togglePlay);

    video.addEventListener('timeupdate', () => {
      const progress = (video.currentTime / video.duration) * 100;
      progressBar.style.width = progress + '%';
    });

    video.addEventListener('ended', () => {
      player.classList.remove('playing');
    });
  </script>
</body>
</html>
